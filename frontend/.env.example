# Backend FastAPI URL (VPS). Set in Vercel → Settings → Environment Variables.
# Frontend live at: https://maizescan.vercel.app
# Leave empty for local dev (Vite proxy handles /predict, /health, /model).
VITE_API_URL=https://your-api-domain.com
