# URL of the deployed FastAPI backend (VPS).
# Set this in Vercel dashboard: Settings → Environment Variables → VITE_API_URL
# Leave empty for local dev (Vite proxy handles /predict, /health, /model).
VITE_API_URL=https://your-api-domain.com
